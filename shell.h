#ifndef SHELL_H
#define SHELL_H

/**
 * path_finder - a function that check if a command is valid
 * @com: the command
 *
 * Return 0 on success
 * 1: on failure
 */
char *path_finder(char *com);

#endif
